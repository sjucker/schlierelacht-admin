Run the TypeScript generator to regenerate `shared/types/rest.ts` from the Java DTOs:

```bash
cd schlierelacht-admin && ./mvnw typescript-generator:generate -f pom.xml
```

Run this command and report the outcome. If it fails, show the relevant error output.
